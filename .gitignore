# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.tar.gz

# Don't need go.sum
go.sum

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Specific binaries
bin/pdns_auth_serial_exporter*
pdns_auth_serial_exporter
